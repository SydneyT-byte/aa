html {
  font-size: 100%;
}

body {
  font-size: 1rem;
  line-height: 1.5;
}

h1 {
  font-size: 2rem;
}

.section-box {
  padding: 1.5rem 1rem;
  border-radius: 0.5rem;
  box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.07);
  background-color: #fff;
}

.page-wrapper {
  width: 90%;
  max-width: 50rem; /* 50rem ~= 800px */
  margin: 0 auto;
}

.fixed-wrapper {
  width: 43.75rem;  /* 700px / 16 = 43.75rem */
  max-width: 43.75rem;
  margin: 0 auto;
}
